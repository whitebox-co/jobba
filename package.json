{
  "name": "jobba",
  "version": "0.0.0",
  "description": "Jobba the Hub",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && chmod -f +x ./dist/bin/*",
    "lint": "tslint . || exit 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "job",
    "hub"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^8.0.28",
    "nodemon": "^1.12.1",
    "tslint": "^5.7.0",
    "typescript": "^2.5.2"
  }
}
