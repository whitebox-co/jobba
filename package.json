{
  "name": "jobba",
  "version": "0.0.0",
  "description": "Jobba the Hub",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && chmod -f +x ./dist/bin/*",
    "clean": "rm -rf ./dist",
    "lint": "tslint . || exit 0",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc -w"
  },
  "keywords": [
    "job",
    "hub"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bull": "^3.3.1"
  },
  "devDependencies": {
    "@types/bull": "^3.0.1",
    "@types/node": "^8.0.34",
    "nodemon": "^1.12.1",
    "tslint": "^5.7.0",
    "typescript": "^2.5.3"
  }
}
